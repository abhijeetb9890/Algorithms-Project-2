Instructions to compile the program:

1) In the terminal, move to the directory of the program.
2) Execute the following command to compile the program: 
	g++ -std=c++11 proj.cpp
3) Execute the following command to run the program:
	./a.out
